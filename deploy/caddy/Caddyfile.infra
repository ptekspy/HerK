paddy.systems {
  encode zstd gzip

  handle_path /pgadmin* {
    reverse_proxy pgadmin:80 {
      header_up X-Script-Name /pgadmin
      header_up X-Forwarded-Prefix /pgadmin
      header_up X-Scheme {scheme}
    }
  }

  handle_path /redisinsight* {
    reverse_proxy redisinsight:5540
  }

  @api path /v1/* /webhooks/* /auth/*
  handle @api {
    reverse_proxy contractguard-api:4001
  }

  handle {
    reverse_proxy contractguard-web:4000
  }
}
